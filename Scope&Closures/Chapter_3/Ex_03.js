// "hiding" items in functions.

function doSomething(a) {
	b = a + doSomethingElse( a * 2 );

	console.log( b * 3 );
}

function doSomethingElse(a) {
	return a - 1;
}

var b;

doSomething( 2 ); // 15

// having var b and doSomethingElse in the global scope is unnecessary and possibly dangerous.