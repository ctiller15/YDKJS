// functions being objects is quite useful. They can have properties.

function a(b,c) {
	/* .. */
}

// this will have a length property set to the number of
// parameters it is declared with.

console.log(a.length);	// 2