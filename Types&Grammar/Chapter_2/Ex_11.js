// Number literals are expressed generally as base-10.

var a = 42;
var b = 42.3;

// The leading portion is optional if 0.

var a = 0.42;
var b = .42;

// The trailing portion of a decimal value is also optional.

var a = 42.0;
var b = 42.;	// valid, but confusing.
